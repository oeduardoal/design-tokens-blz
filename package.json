{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build --parallel",
    "dev": "lerna run dev --stream",
    "start": "lerna run start --stream"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "dependencies": {
    "@belezanaweb/uai": "^0.16.0"
  }
}
